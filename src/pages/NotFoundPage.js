import React from "react";
import Dokumentasi from "../NotFoundPage/dokumentasi";

const NotFoundPage = () => {
  return (
    <div>
      <Dokumentasi/>
    </div>
  )
};

export default NotFoundPage;
